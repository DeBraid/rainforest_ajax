<%= link_to "Add a product", new_product_path %>

<h1>Products</h1> 
<h2><small><%= page_entries_info @products %></small></h2>

<hr/>

<ul>
  <div id="products">
    <%= render @products %>
  </div>
</ul>


<%= paginate @products %>