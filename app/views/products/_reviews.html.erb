<% @product.reviews.each do |review| %>
  <div class="review">
    <div class="poster">Posted by <%= review.user.username_string %> on <%= review.created_at %></div>
    <p class="comment">
    <%= review.comment %>
    </p>
  </div>
<% end %>